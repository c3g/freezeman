import React from 'react'

// The page that lists the samples ready for processing by a given
// protocol and allows the user to select the samples and generate
// a prefilled template.
const LabworkProtocolPage = ({}) => {
	
}